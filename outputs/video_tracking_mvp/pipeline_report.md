# Broadcast video to pitch coordinates MVP

Status: **BLOCKED**

REAL VIDEO E2E: **BLOCKED**

NO RIGHTS-CLEARED INPUT

Ground point: ((x_min+x_max)/2, y_max), in original decoded image pixels.

Source uncertainty fields stay separate. Canonical confidence and player identity remain NULL.

- Broadcast visible subset only; missing detection does not establish player absence.
- Bottom-centre bbox is a ground-contact approximation affected by pose, occlusion, camera angle and box error.
- Reprojection residual is landmark fit error in pixels, not player coordinate accuracy or a probability.
- Four exact landmarks have no redundant fit validation; extrapolation beyond their hull is flagged.
- Manual whole-segment static-camera review and endpoint checks cannot prove absence of between-check drift.
- Anonymous short tracks can fragment or switch identity; no cross-cut/replay re-identification.
- Person detector is not football-specific; manual review must exclude referees, staff and spectators.
- All spatial features describe incompletely observed players; nearest distances and team extents are poorly supported.
- 105x68m is the canonical reference; actual pitch dimensions must be accounted for when entering landmarks.

See quality_summary.json for actual counts and run metadata. No real measurements exist when counts are NULL.

## Implemented and executed component evidence

Source: `quality_summary.json`, generated by the CLI with `--self-check --detector-smoke`.
These are synthetic software contracts, not broadcast measurements.

- Detector: TorchVision 0.29.0+cpu SSDLite320 MobileNet V3 Large, COCO_V1.
  Official weight SHA-256 was checked; actual CPU inference ran on one generated
  blank image and retained zero person detections. Accuracy was not evaluated.
- Tracker: Supervision 0.27.0 ByteTrack ran on prescribed synthetic detections.
- Synthetic adapter fixture: 3 frames, 4 detections, 2 tracks, 4 canonical rows;
  frame 1 had no detections and no positions were fabricated.
- Synthetic calibration: 8 correspondences, 8 inliers; mean/median/max image
  reprojection residuals = 2.325201e-6 / 2.319153e-6 / 4.231771e-6 px.
  Maximum independent query error = 4.950814e-7 m. These tiny residuals arise from
  generated exact geometry and do not predict any real-video measurement error.
- Synthetic canonical rows were accepted by the unchanged validator and existing
  player/frame/window and team/frame/window functions. Anonymous identities and
  unknown eligible-frame coverage remain NULL. All transformed fixture points were
  inside the canonical pitch; that is not a real-video in-pitch rate.
- Team assignment and attacking direction contracts passed using programmatically
  supplied manual-review fields. No human-reviewed real-video team labels exist.

## Verification

Windows Python 3.12, `MPLBACKEND=Agg`, UTF-8 mode:

```powershell
.venv/Scripts/python.exe -X utf8 -B -m pytest -q -p no:cacheprovider
```

Result: **179 passed, 20 subtests passed, 0 failed, 0 errors**.
The unchanged baseline had 127 tests plus 20 subtests; 52 new tests cover geometry,
tracking, decoding, persistence, rights, output paths and existing-engine contracts.
Initial baseline cp949 and Tcl failures were resolved with the execution options
above; protected code was not edited. `pip check` found no broken requirements.

Independent review's input-footage storage finding was reproduced and fixed.
The engine source and all six protected paths have no diff against the baseline
commit `6037376128375df4d00477b0e300ee36d94b83fe`.
Raw video, weight cache and annotated output paths were confirmed git-ignored.
Work remains uncommitted on `feature/spatial-engine-v1`.

## Reproduce and continue

Read [the command/config guide](../../docs/video_tracking_mvp.md) and use
`config/video_sample.template.json`. With a permitted 20–30s continuous clip,
run review mode, complete team mapping/manual landmarks/camera review, then execute
the documented one-command pipeline into a fresh ignored directory.

**NEXT MINIMUM ACTION:** supply a rights-cleared clip and complete that review config.
**REAL VIDEO E2E: BLOCKED — NO RIGHTS-CLEARED INPUT.**
