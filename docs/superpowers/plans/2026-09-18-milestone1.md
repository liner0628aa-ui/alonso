# Milestone 1 implementation plan

Use the user's detailed Milestone 1 specification as the approved scope. Work in the existing project; no repository replacement. No model, UI or next milestone.

1. Audit official competitions and all 34 Bundesliga 2023/24 Alonso fixtures using the existing cache. Record absence of 2022/23, previous IDs and per-match actual file availability.
2. Keep inspect_360 strict for existing callers/tests; add an optional boundary used by ingestion that records invalid frames without blocking events. Test invalid UUID handling first. Run existing CLI with --match-limit 34 (the full audited catalogue), preserving schema, validation and attribution.
3. Add src/analysis/wirtz_role_mvp.py consuming read_dataset and receipt-checked raw qualifiers. Tests cover completed receipts, touch proxy exclusion, directional/zone boundaries, failed pass exclusion, missing coordinates and minutes. Define local normalized-x progression threshold explicitly; do not imply provider equivalence.
4. Generate per-match and pooled metrics, event-level action/shot audit CSVs, five required charts plus variation/team context if cheap. All summary numbers computed in code; unknown minutes and 360 remain unavailable.
5. Update README, feature definitions and data audit. Run full unittest suite, processed checksum validation, inspect actual charts and review code. Stop at Milestone 1.
