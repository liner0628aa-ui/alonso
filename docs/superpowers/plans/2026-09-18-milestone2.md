# Milestone 2 implementation plan

Scope: implement the user's approved player-match/team PCA baseline in the existing project. No ingestion changes, clustering, prototypes, external players, 360 features or neural networks.

1. Reuse wirtz_role_mvp.metrics for every Leverkusen lineup row; extract its raw shot-assist/xG calculation into a shared helper without changing Wirtz output definitions. Preserve unused/short/unknown-minute rows with explicit eligibility reasons. Add minimal tests for rates, exclusions and weighted aggregation.
2. Save all team rows with raw counts, per90 and metadata. Baseline fit: known match minutes >=30, player season known minutes >=450, complete selected core vector. Goalkeepers are included if eligible; identify their nominal position and disclose their influence on the team-wide baseline.
3. Use 15 non-identifying features: average touch x/y, total half-space share, passes p90, forward pass fraction, progressive passes/carries p90, final-third/box entries p90, shot assists p90, xG p90, pressures/recoveries/tackles/interceptions p90. Preserve receptions, carries, shots and other spatial metrics outside the vector to avoid redundant weighting.
4. Fit StandardScaler only on eligible player-match rows; fit full deterministic PCA there. Season vector = minute-weighted mean of eligible match vectors; rates therefore equal sum counts / eligible minutes *90. Save unweighted median/std, sample size and all-known vs represented minutes. Similarity and stability use all selected standardized dimensions, not PCA2. Export cosine distributions and centroid-inclusive bias warning, plus leave-one-match-out stability when possible.
5. Generate six required figures plus profile table, all required CSV/Parquet tables, dictionary, scaler/PCA provenance, report and exclusions. Verify real Wirtz counts match Milestone 1, PCA algebra/similarity bounds, original data hashes, all tests and visual readability. Independent code review before completion. Stop.
